do 
function run(msg,matches)
  if matches[1]:lower() == "^(.*)$"
  return "/add or s"
    if matches[1]:lower() == "^(.*)$"
  return "/add or aa"
end
return {
  description = " spammer ", 
  patterns = {
    "^(.*)$",
  },
  run = run
}
end
